streamlit>=1.20
pandas>=1.5
numpy>=1.24
plotly>=5.15
scikit-learn>=1.2
requests>=2.28
# pmdarima 2.1+ requires Python >=3.11; pin to a version compatible with Python 3.10
pmdarima==2.0.4
# Prophet (fbprophet) has heavy build requirements on Windows. Install via conda if needed:
# conda install -c conda-forge prophet

# Optional (only if you need these features):
# pyarrow
# geopandas
# xarray
